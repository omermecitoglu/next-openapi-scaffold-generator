import { describe, expect, it, vi } from "vitest";
import { zocker } from "zocker";
import db from "~/database";
import { {{ pascalCase modelName }}PatchDTO, {{ pascalCase modelName }}DTO } from "~/models/{{ kebabCase modelName }}";
import update{{ pascalCase modelName }} from "./update{{ pascalCase modelName }}";

describe("update{{ pascalCase modelName }}", () => {
  function generateMockPatch() {
    const zock = zocker({{ pascalCase modelName }}PatchDTO);
    let mock: ReturnType<typeof zock.generate>;
    do {
      mock = zock.generate();
    } while (Object.values(JSON.parse(JSON.stringify(mock))).length < 1); // because .set(patch) might throw "No values to set"
    return mock;
  }

  it("should update a {{ noCase modelName }} in the database", async () => {
    const mockData = zocker({{ pascalCase modelName }}DTO.pick({ id: true })).generate();
    const mockPatch = generateMockPatch();
    const spy = vi.spyOn(db.$client, "query").mockImplementation(() => Promise.resolve({ rows: [], rowCount: 0 }));

    await update{{ pascalCase modelName }}(db, mockData.id, mockPatch);

    expect(spy).toHaveBeenCalled();
    const [query, parameters] = spy.mock.calls[0] as unknown as [{ text: string }, string[]];
    const q = /^update "{{ snakeCase modelName "plural" }}" set (.+) where (.+) returning (.+)$/;
    expect(query.text).toMatch(q);
    expect(parameters).toEqual([
      ...Object.values(mockPatch).filter(p => typeof p !== "undefined"),
      mockData.id,
    ]);

    spy.mockRestore();
  });
});
