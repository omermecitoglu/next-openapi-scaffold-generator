import { describe, expect, it } from "vitest";
import { zocker } from "zocker";
import { db } from "~/database";
import { {{ pascalCase modelName }}DTO, New{{ pascalCase modelName }}DTO } from "~/models/{{ kebabCase modelName }}";
import create{{ pascalCase modelName }} from "./create{{ pascalCase modelName }}";
import get{{ pascalCase modelName }} from "./get{{ pascalCase modelName }}";

describe("get{{ pascalCase modelName }}", () => {
  it("should return undefined when no {{ noCase modelName }} is found with the given id", async () => {
    const mock{{ pascalCase modelName }}Id = zocker({{ pascalCase modelName }}DTO.pick({ id: true })).generate().id;
    const {{ camelCase modelName }} = await get{{ pascalCase modelName }}(db, mock{{ pascalCase modelName }}Id);
    expect({{ camelCase modelName }}).toBeUndefined();
  });

  it("should return the {{ noCase modelName }} when a {{ noCase modelName }} is found with the given id", async () => {
    const created{{ pascalCase modelName }} = await create{{ pascalCase modelName }}(db, {
      ...zocker(New{{ pascalCase modelName }}DTO).generate(),
    });

    const {{ camelCase modelName }} = await get{{ pascalCase modelName }}(db, created{{ pascalCase modelName }}.id);
    expect({{ camelCase modelName }}).not.toBeUndefined();
    expect({{ camelCase modelName }}?.id).toBe(created{{ pascalCase modelName }}.id);
  });
});
