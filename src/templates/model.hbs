import { createInsertSchema, createSelectSchema, createUpdateSchema } from "drizzle-zod";
import { createSharedRefinements } from "drizzle-zod-shared-refinements";
import { {{ camelCase modelName "plural" }} } from "~/database/schema/{{ kebabCase modelName "plural" }}";
import type z from "zod";

const sharedRefinements = createSharedRefinements({{ camelCase modelName "plural" }}, {
  id: schema => schema.readonly().describe("Unique identifier of the {{ noCase modelName }}"),
  // describe other fields here
  createdAt: schema => schema.readonly().describe("Creation date of the {{ noCase modelName }} as an ISO 8601 date string"),
  updatedAt: schema => schema.readonly().describe("Modification date of the {{ noCase modelName }} as an ISO 8601 date string"),
});

export const {{ pascalCase modelName }}DTO = createSelectSchema({{ camelCase modelName "plural" }}, sharedRefinements).describe("Represents a {{ noCase modelName }} definition");

export const New{{ pascalCase modelName }}DTO = createInsertSchema({{ camelCase modelName "plural" }}, sharedRefinements).omit({
  id: true,
  createdAt: true,
  updatedAt: true,
}).describe("Data Transfer Object for creating a new {{ noCase modelName }}");

export const {{ pascalCase modelName }}PatchDTO = createUpdateSchema({{ camelCase modelName "plural" }}, sharedRefinements).omit({
  id: true,
  createdAt: true,
  updatedAt: true,
}).describe("Data Transfer Object for updating an existing {{ noCase modelName }}");

export type {{ pascalCase modelName }} = z.infer<typeof {{ pascalCase modelName }}DTO>;
export type New{{ pascalCase modelName }} = z.infer<typeof New{{ pascalCase modelName }}DTO>;
export type {{ pascalCase modelName }}Patch = z.infer<typeof {{ pascalCase modelName }}PatchDTO>;
