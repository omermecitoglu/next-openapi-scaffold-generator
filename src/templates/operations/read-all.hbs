import type database from "~/database";
import { type {{ pascalCaseSingular }}DTO } from "~/models/{{ kebabCaseSingular }}";
import selectColumns from "~/utils/column";

export default function get{{ pascalCasePlural }}(db: Omit<typeof database, "$client">, select: (keyof {{ pascalCaseSingular }}DTO)[] = []) {
  return db.query.{{ camelCasePlural }}.findMany({
    columns: selectColumns(select),
    orderBy: (u, { asc }) => [asc(u.createdAt)],
  });
}
