import { eq } from "drizzle-orm";
import { type db as database } from "~/database";
import { {{ camelCase modelName "plural" }} } from "~/database/schema/{{ kebabCase modelName "plural" }}";
import { type {{ pascalCase modelName }}Patch } from "~/models/{{ kebabCase modelName }}";

export default async function update{{ pascalCase modelName }}(db: Omit<typeof database, "$client">, {{ camelCase modelName }}Id: string, patch: {{ pascalCase modelName }}Patch) {
  const results = await db.update({{ camelCase modelName "plural" }})
    .set(patch)
    .where(eq({{ camelCase modelName "plural" }}.id, {{ camelCase modelName }}Id))
    .returning();
  return results.at(0);
}
