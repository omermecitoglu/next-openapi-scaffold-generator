import type database from "~/database";

export default function get{{ pascalCase modelName }}(db: Omit<typeof database, "$client">, {{ camelCase modelName }}Id: string) {
  return db.query.{{ camelCase modelName "plural" }}.findFirst({
    where: {
      id: {{ camelCase modelName }}Id,
    },
  });
}
